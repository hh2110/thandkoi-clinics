# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env
.env.*
!.env.example
*.local

# Django / Wagtail
*.sqlite3
/media/
/staticfiles/

# Data — never commit patient data or raw exports
*.xls
*.xlsx
# The clinic software can also emit patient-level reports as CSV (observed
# 2026-07-25 while grounding Plan 16) — same invariant #5 rule as the Excel
# exports above. No CSV is tracked in this repo; a deliberate fixture can
# still be added with `git add -f`.
*.csv
/uploads/
/data/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local skill state — may reference personal chat/contact names
/.claude/state/

# Generated by scripts/generate_og_cards.py — converted webfonts, not source.
/scripts/.fontcache/
